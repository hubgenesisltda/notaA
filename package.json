{
  "name": "nota-a-edge",
  "version": "1.0.0",
  "description": "Nota A — Edge Functions (proxy Anthropic API)",
  "private": true,
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "scripts": {
    "dev":    "vercel dev",
    "deploy": "vercel --prod",
    "test":   "node --experimental-vm-modules node_modules/.bin/jest"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
